machine:
    java:
        version: openjdk7

dependencies:
    override:
        - ./gradlew setupciworkspace

compile:
    override:
        - ./gradlew build

general:
    artifacts:
        - "build/libs"